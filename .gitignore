.idea
__pycache__/

# build output (mkdocs build)
site/

# SANDARC-VEC-Manual.pdf is generated by scripts/build-pdf.py but IS
# committed: Cloudflare's git-connected build only runs `mkdocs build`,
# and hooks/copy_pdf.py ships the checked-in PDF with the site.
# Regenerate and commit it when docs change.

# wrangler files
.wrangler
.dev.vars*
!.dev.vars.example
.env*
!.env.example
